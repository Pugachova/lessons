//console.log('Pugachova');

/*let one = 'apple';
let two = 'green';
console.log(one, two); 

one = two;
console.log(one, two); */
 
/*4. 
	let obj = {
	a: "alphabet", //String 
	b: 15,//Number
	c: true,//Boolean (true and false)
	//d:   // Undefined 
    e: null// Null 
};
console.log(obj['a'], obj['b'], obj['c'], obj['d'], obj['e']); */


/*5. let isAdult = confirm("Чи Вам є 18 років?");
   console.log(isAdult);  // true, если нажата OK   */
 
/* 6.
let name = 'Evgenia';//String 
let surName = 'Pugachova'; //String 
let group = 533;//Number
let year = 1986; //Number
let status = false;  //Boolean

console.log(group, year, status, name, surName);

let example = null;
let app;
console.log(example, app);   */

/*7.
let name = prompt('Ваше ім"я?');
let mail = prompt('Ваш email');
let pass = prompt('Ваш пароль');
alert('Ваше имя: ' + name);
alert('Ваш email: ' + mail);
alert('Ваш пароль: ' + pass);  */

/* 8.
let hour = 60 * 60;
alert(hour); // кількість секунд в годині

let day = 60 * 60 * 24;
alert(day); // кількість секунд в добі

let month = 60 * 60 * 24 * 30;
alert(month); // кількість секунд в місяці */






/*
 
 let count = 70;
          let min = 10;
            let max = 50;
           let  arr = [];
            let result = [];
 
        for (let i = 0; i < count; i++) {
            arr[i] = Math.floor(Math.random() * (max - min + 1) + min);
        }
        for (let i = min; i <= max; i++) {
            if (arr.indexOf(i) < 0)
                result.push(i);
        }
        console.log(arr);
        console.log(result);
		
*/



 


// return Math.floor(Math.random() * Math.floor(k));
//}
//console.log(randArray(5)); // expected output: 0, 1, 2, or 4
/*function randArray(k){
var arr2 = [];

for (let i = 1; i < 500; i++ ) {

    arr2.push( Math.round( Math.random() * 100 ));

} 
}
randArray();*/



